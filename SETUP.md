# Инструкция по настройке и запуску

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
# Клонируем репозиторий
git clone <your-repo-url>
cd ml-interactive-course

# Устанавливаем зависимости для бэкенда
cd backend
pip install -r requirements.txt

# Устанавливаем зависимости для фронтенда
cd ../frontend
npm install
```

### 2. Настройка базы данных

```bash
# Переходим в папку бэкенда
cd backend

# Инициализируем базу данных с начальными данными
python init_db.py
```

### 3. Запуск приложения

```bash
# Запускаем бэкенд (в одном терминале)
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Запускаем фронтенд (в другом терминале)
cd frontend
npm run dev
```

## 📋 Подробная настройка

### Backend (FastAPI)

#### Требования
- Python 3.8+
- pip

#### Зависимости
Основные пакеты:
- `fastapi` - веб-фреймворк
- `sqlalchemy` - ORM для работы с БД
- `scikit-learn` - ML библиотека
- `numpy`, `pandas` - работа с данными
- `plotly` - генерация графиков

#### Структура API
- `/api/auth/*` - аутентификация и регистрация
- `/api/lessons/*` - управление уроками и прогрессом
- `/api/ml/*` - ML симуляторы

#### База данных
- SQLite (для разработки)
- Автоматическое создание таблиц
- Начальные данные: 3 урока + вопросы

### Frontend (Next.js)

#### Требования
- Node.js 18+
- npm

#### Зависимости
- `next` - React фреймворк
- `tailwindcss` - CSS фреймворк
- `plotly.js` - интерактивные графики
- `lucide-react` - иконки
- `react-hook-form` - формы

#### Структура
- `/app/*` - страницы (App Router)
- `/components/*` - переиспользуемые компоненты
- `/app/lesson/[id]` - страница урока
- Главная страница с презентацией курса

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в папке `backend`:

```env
# JWT секрет (измените в продакшене!)
SECRET_KEY=your-super-secret-key-here

# База данных
DATABASE_URL=sqlite:///./ml_course.db

# CORS настройки
FRONTEND_URL=http://localhost:3000
```

### Настройка CORS

В `backend/main.py` настроен CORS для `http://localhost:3000`.

## 📚 Использование

### 1. Регистрация пользователя
- Перейдите на `/register`
- Создайте аккаунт с email и паролем

### 2. Авторизация
- Войдите в систему на `/login`
- Получите JWT токен

### 3. Изучение курса
- Начните с первого урока
- Изучайте теорию
- Экспериментируйте с симуляторами
- Отмечайте прогресс

### 4. Интерактивные симуляторы
- **Линейная регрессия**: изменяйте параметры в реальном времени
- Наблюдайте за изменением графика
- Изучайте метрики качества

## 🐛 Отладка

### Backend
```bash
# Логи FastAPI
uvicorn main:app --reload --log-level debug

# Проверка API
curl http://localhost:8000/health
curl http://localhost:8000/docs  # Swagger UI
```

### Frontend
```bash
# Режим разработки с подробными логами
npm run dev

# Проверка в браузере
# http://localhost:3000
```

### База данных
```bash
# Просмотр SQLite БД
sqlite3 backend/ml_course.db

# Основные таблицы
.tables
SELECT * FROM lessons;
SELECT * FROM users;
```

## 📁 Структура проекта

```
ml-interactive-course/
├── backend/                 # FastAPI сервер
│   ├── main.py             # Основное приложение
│   ├── database.py         # Настройка БД
│   ├── models.py           # Модели SQLAlchemy
│   ├── schemas.py          # Pydantic схемы
│   ├── routers/            # API роутеры
│   │   ├── auth.py         # Аутентификация
│   │   ├── lessons.py      # Уроки
│   │   └── ml_simulator.py # ML симуляторы
│   ├── init_db.py          # Инициализация БД
│   └── requirements.txt    # Python зависимости
├── frontend/               # Next.js приложение
│   ├── app/                # Страницы (App Router)
│   │   ├── page.tsx        # Главная страница
│   │   ├── lesson/[id]/    # Страница урока
│   │   ├── layout.tsx      # Корневой layout
│   │   └── globals.css     # Глобальные стили
│   ├── components/         # React компоненты
│   │   └── LinearRegressionSimulator.tsx
│   ├── package.json        # Node.js зависимости
│   └── tailwind.config.js  # Конфигурация Tailwind
├── README.md               # Описание проекта
└── SETUP.md               # Эта инструкция
```

## 🚀 Деплой

### Локальный тест
```bash
# Backend
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000

# Frontend
cd frontend
npm run build
npm start
```

### Продакшен
- **Backend**: Docker + любой хостинг (Render, Railway, AWS)
- **Frontend**: Vercel, Netlify, или статический хостинг
- **База данных**: PostgreSQL (вместо SQLite)

## 🤝 Разработка

### Добавление новых симуляторов
1. Создайте роутер в `backend/routers/ml_simulator.py`
2. Добавьте схемы в `backend/schemas.py`
3. Создайте React компонент в `frontend/components/`
4. Интегрируйте в страницу урока

### Добавление новых уроков
1. Отредактируйте `backend/init_db.py`
2. Добавьте контент урока
3. Создайте вопросы (опционально)
4. Запустите `python init_db.py`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в терминале
2. Убедитесь, что все зависимости установлены
3. Проверьте, что порты 8000 и 3000 свободны
4. Проверьте настройки CORS и JWT

## 🔮 Планы развития

- [ ] Добавление новых ML алгоритмов
- [ ] Система тестирования
- [ ] Геймификация (бейджи, достижения)
- [ ] Социальные функции (комментарии, обсуждения)
- [ ] Мобильное приложение
- [ ] Интеграция с внешними ML платформами
